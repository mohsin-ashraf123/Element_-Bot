# ?? Web framework ?????????????????????????????????????????
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ?? Config / settings ?????????????????????????????????????
pydantic-settings==2.7.1
python-dotenv==1.0.1
tzdata>=2024.1

# ?? Database (PostgreSQL) ?????????????????????????????????
sqlalchemy>=2.0
psycopg2-binary==2.9.10
alembic>=1.13

# ?? Security (auth + secret encryption) ???????????????????
passlib[argon2]>=1.7.4
argon2-cffi>=23.1
cryptography>=42.0
pyjwt>=2.8

# ?? HTTP client (LLM adapters, later) ?????????????????????
httpx>=0.27

# ?? Element E2EE (matrix-nio + python-olm) ??????????
# python-olm is installed in Dockerfile from GitLab Matrix.org wheels (not PyPI sdist)
matrix-nio>=0.25
cachetools>=5.3
peewee>=3.14
atomicwrites>=1.4
base58>=2.1                 # decode Element recovery key (SSSS cross-signing recovery)
pynacl>=1.5                 # ed25519 sign/verify for device cross-signing
playwright>=1.49            # HTML report table → PNG screenshot

# ?? Testing ???????????????????????????????????????????????
pytest>=8.0

# ?? Deferred (installed when their phase starts) ??????????
# matrix-nio[e2e]      # Phase 1  ? Element E2EE client (installed: matrix-nio + python3-olm)
# celery               # Phase 2  ? job scheduling
# redis                # Phase 2  ? broker + cache
# playwright           # installed — HTML→PNG report renderer
